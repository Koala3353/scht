import { AiWorkbench } from '@/components/ai/ai-workbench';
import { PageHeader } from '@/components/workspace/page-header';

export default function AiPage() { return <main><PageHeader eyebrow="AI ASSISTANT" title="Propose, review, apply">Scht never writes to your planner automatically. Review every suggested task before applying it.</PageHeader><AiWorkbench /></main>; }
